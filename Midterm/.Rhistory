}
temp<-temp+1
}
}
}
View(linked)
which(linked==1)
linked<-matrix(NA, nrow=250, ncol=250)
linked.codes<-linked.movies("tt0110357")
for (i in 1:250)){
temp <- 1
while (temp<=12){
for (j in 1:250){
index<-linked.codes[temp]
if (top250.codes[j] == index){
linked[j,i]<-1
}
}
temp<-temp+1
}
}
linked<-matrix(NA, nrow=250, ncol=250)
linked.codes<-linked.movies("tt0110357")
for (i in 1:250)){
temp <- 1
while (temp<=12){
for (j in 1:250)){
index<-linked.codes[temp]
if (top250.codes[j] == index){
linked[j,i]<-1
}
}
temp<-temp+1
}
}
linked<-matrix(NA, nrow=250, ncol=250)
linked.codes<-linked.movies("tt0110357")
for (i in 1:250){
temp <- 1
while (temp<=12){
for (j in 1:250){
index<-linked.codes[temp]
if (top250.codes[j] == index){
linked[j,i]<-1
}
}
temp<-temp+1
}
}
which(linked==1)
View(linked)
linked<-matrix(NA, nrow=250, ncol=250)
linked.codes<-linked.movies("tt0110357")
for (i in 1:250){
temp <- 1
while (temp<=12){
index<-linked.codes[temp]
for (j in 1:250){
if (top250.codes[j] == index){
linked[j,i]<-1
}
}
temp<-temp+1
}
}
which(linked==1)
linked<-matrix(NA, nrow=250, ncol=250)
linked.codes<-linked.movies("tt0110357")
for (i in 1:250){
temp <- 1
while (temp<=12){
index<-linked.codes[temp]
for (j in 1:250){
if (top250.codes[j] == index){
linked[i,j]<-1
}
}
temp<-temp+1
}
}
which(linked==1)
View(linked)
linked<-matrix(NA, nrow=250, ncol=250)
linked.codes<-linked.movies("tt0110357")
for (i in 1:250){
temp <- 1
while (temp<=12){
index<-linked.codes[temp]
for (j in 1:250){
if (top250.codes[j] == index){
linked[i,j]<-1
}
else{
linked[i,j]<-0
}
}
temp<-temp+1
}
}
which(linked==1)
linked<-matrix(NA, nrow=250, ncol=250)
linked.codes<-linked.movies("tt0110357")
for (i in 1:250){
temp <- 1
while (temp<=12){
index<-linked.codes[temp]
for (j in 1:250){
if (top250.codes[j] == index){
linked[i,j]<-1
}
else if (top250.codes[j] != index)
linked[i,j]<-0
}
}
temp<-temp+1
}
which(linked==1)
View(linked)
top250.codes[98]
linked<-matrix(NA, nrow=250, ncol=250)
linked.check<-function(imdb.code){
linked.codes<-linked.movies(imdb.code)
for (i in 1:250){
temp <- 1
while (temp<=12){
index<-linked.codes[temp]
for (j in 1:250){
if (top250.codes[j] == index){
linked[i,j]<-1
}
else if (top250.codes[j] != index)
linked[i,j]<-0
}
}
temp<-temp+1
}
}
}
linked<-matrix(NA, nrow=250, ncol=250)
linked.check<-function(imdb.code){
linked.codes<-linked.movies(imdb.code)
for (i in 1:250){
temp <- 1
while (temp<=12){
index<-linked.codes[temp]
for (j in 1:250){
if (top250.codes[j] == index){
linked[i,j]<-1
}
else if (top250.codes[j] != index)
linked[i,j]<-0
}
}
temp<-temp+1
}
}
linked<-matrix(NA, nrow=250, ncol=250)
linked.check<-function(imdb.code){
linked.codes<-linked.movies(imdb.code)
for (i in 1:250){
temp <- 1
while (temp<=12){
index<-linked.codes[temp]
for (j in 1:250){
if (top250.codes[j] == index){
linked[i,j]<-1
}
else if (top250.codes[j] != index)
linked[i,j]<-0
}
}
temp<-temp+1
}
return (linked)
}
link <- sapply(top250.codes[1:5],linked.check)
which(linked==1)
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
View(linked)
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
ronames(linked)<-top250.codes
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
View(linked)
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
linked.codes<-linked.movies("tt0110357")
if (any(linked.codes)==rownames){
cat(1)
}
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
linked.codes<-linked.movies("tt0110357")
if (any(linked.codes)==rownames(linked)){
cat(1)
}
which(linked.codes==rownames(linked))
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
linked.codes<-linked.movies("tt0110357")
linked["tt0110357",linked.codes]<-1
linked["tt0110357",all(linked.codes)]<-1
which(linked==1)
temp <- 1
while (temp<=12){
linked["tt0110357",linked.codes[temp]]<-1
temp<-temp+!
}
temp <- 1
while (temp<=12){
linked["tt0110357",linked.codes[temp]]<-1
temp<-temp+1
}
which(linked==1)
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
linked.codes<-linked.movies("tt0110357")
temp <- 1
while (temp<=length(linked.codes)){
linked["tt0110357","linked.codes[temp]"]<-1
temp<-temp+1
}
temp <- 1
while (temp<=length(linked.codes)){
linked["tt0110357",linked.codes[temp]]<-1
temp<-temp+1
}
linked.code[10]
linked.codes[10]
length(linked.codes)
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
linked.codes<-linked.movies("tt0110357")
temp <- 1
while (temp<=length(linked.codes)){
linked["tt0110357",linked.codes[temp]]<-1
temp<-temp+1
}
temp <- 1
while (temp<=length(linked.codes)){
val<-linked.codes[temp]
linked["tt0110357",val]<-1
temp<-temp+1
}
which(linked==1)
View(linked)
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
linked.codes<-linked.movies("tt0114709")
temp <- 1
while (temp<=length(linked.codes)){
val<-linked.codes[temp]
linked["tt0110357",val]<-1
temp<-temp+1
}
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
linked.codes<-linked.movies("tt0114709")
temp <- 1
while (temp<=length(linked.codes)){
val<-linked.codes[temp]
linked["tt0114709",val]<-1
temp<-temp+1
}
length(rownames(linked))
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
linked.codes<-linked.movies("tt0114709")
temp <- 1
while (temp<=length(linked.codes)){
val<-linked.codes[temp]
linked[val,"tt0114709"]<-1
temp<-temp+1
}
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
linked.codes<-linked.movies("tt0114709")
temp <- 1
while (temp<=length(linked.codes)){
val<-linked.codes[temp]
linked[val,"tt0114709"]<-1
linked["tt0114709",val]<-1
temp<-temp+1
}
linked.codes
temp <- 1
while (temp<=length(linked.codes)){
val<-linked.codes[temp]
cat(val)
linked[val,"tt0114709"]<-1
linked["tt0114709",val]<-1
temp<-temp+1
}
temp <- 1
while (temp<=length(linked.codes)){
val<-linked.codes[temp]
cat(val,"it is")
linked[val,"tt0114709"]<-1
linked["tt0114709",val]<-1
temp<-temp+1
}
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
linked.codes<-linked.movies("tt0114709")
temp <- 1
while (temp<=length(linked.codes)){
val<-linked.codes[temp]
if (rownames(linked)==val){cat(yes)}}
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
linked.codes<-linked.movies("tt0114709")
match(linked.codes[1],rownames(linked))
match(linked.codes[3],rownames(linked))
match(linked.codes[4],rownames(linked))
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
linked.codes<-linked.movies("tt0114709")
temp <- 1
while (temp<=length(linked.codes)){
val<-linked.codes[temp]
if(!is.na(match(val,rownames(linked)))){
linked[val,"tt0114709"]<-1
linked["tt0114709",val]<-1
}
temp<-temp+1
}
which(linked==1)
match(linked.codes,rownames(linked))
!is.nae(match(linked.codes,rownames(linked)))
!is.na(match(linked.codes,rownames(linked)))
linked[linked.codes,"tt0114709"]<-1
linked.codes<-linked.codes[which(!is.nae(match(linked.codes,rownames(linked))))]
linked.codes<-linked.codes[which(!is.na(match(linked.codes,rownames(linked))))]
linked.codes
linked[linked.codes[1:length(linked.codes)],"tt0114709"]<-1
which(linked==1)
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
linked.codes<-linked.movies("tt0114709")
linked.codes<-linked.codes[which(!is.na(match(linked.codes,rownames(linked))))]
linked[linked.codes[1:length(linked.codes)],"tt0114709"]<-1
which(linked==1)
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
linked.check<-function(imdb.code){
linked.codes<-linked.movies(imdp.code)
linked.codes<-linked.codes[which(!is.na(match(linked.codes,rownames(linked))))]
linked[linked.codes[1:length(linked.codes)],imdb.code]<-1
linked[imdb.code,linked.codes[1:length(linked.codes)]]<-1
}
linked.check("tt0114709")
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
linked.check<-function(imdb.code){
linked.codes<-linked.movies(imdb.code)
linked.codes<-linked.codes[which(!is.na(match(linked.codes,rownames(linked))))]
linked[linked.codes[1:length(linked.codes)],imdb.code]<-1
linked[imdb.code,linked.codes[1:length(linked.codes)]]<-1
}
linked.check("tt0114709")
which(liked==1)
which(linked==1)
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
linked.check<-function(imdb.code){
linked.codes<-linked.movies(imdb.code)
linked.codes<-linked.codes[which(!is.na(match(linked.codes,rownames(linked))))]
linked[linked.codes[1:length(linked.codes)],imdb.code]<-1
linked[imdb.code,linked.codes[1:length(linked.codes)]]<-1
return(linked)
}
linked.check("tt0114709")
which(linked==1)
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
#linked.check<-function(imdb.code){
linked.codes<-linked.movies("tt0114709")
linked.codes<-linked.codes[which(!is.na(match(linked.codes,rownames(linked))))]
linked[linked.codes[1:length(linked.codes)],"tt0114709"]<-1
linked["tt0114709",linked.codes[1:length(linked.codes)]]<-1
which(linked==1)
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
linked.check<-function(imdb.code){
linked.codes<-linked.movies(imdb.code)
linked.codes<-linked.codes[which(!is.na(match(linked.codes,rownames(linked))))]
linked[linked.codes[1:length(linked.codes)],imdb.code]<-1
linked[imdb.code,linked.codes[1:length(linked.codes)]]<-1
return(linked)
}
linked.check("tt0114709")
which(linked==1)
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
#linked.check<-function(imdb.code){
linked.codes<-linked.movies("tt0114709")
linked.codes<-linked.codes[which(!is.na(match(linked.codes,rownames(linked))))]
linked[linked.codes[1:length(linked.codes)],"tt0114709"]<-1
linked["tt0114709",linked.codes[1:length(linked.codes)]]<-1
#  return(linked)
#}
}
linked[which(linked==1)]
which(linked==1)
rownames(linked[which(linked==1)])
View(linked)
View(data)
linked.check<-function(imdb.code){
linked.codes<-linked.movies(imdb.code)
linked.codes1<-linked.codes[which(!is.na(match(linked.codes,rownames(linked))))]
linked[linked.codes1[1:length(linked.codes1)],imdb.code]<-1
linked[imdb.code,linked.codes1[1:length(linked.codes1)]]<-1
return(linked)
}
linked.check("tt0114709")
linked.check("tt0114709")
which(linked==1)
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
#linked.check<-function(imdb.code){
linked.codes<-linked.movies("tt0114709")
linked.codes<-linked.codes[which(!is.na(match(linked.codes,rownames(linked))))]
linked[linked.codes[1:length(linked.codes)],"tt0114709"]<-1
linked["tt0114709",linked.codes[1:length(linked.codes)]]<-1
linked(which==1)
which(linked==1)
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
linked.check<-function(imdb.code){
linked.codes<-linked.movies(imdb.code)
linked.codes1<-linked.codes[which(!is.na(match(linked.codes,rownames(linked))))]
linked[linked.codes1[1:length(linked.codes1)],imdb.code]<-1
linked[imdb.code,linked.codes1[1:length(linked.codes1)]]<-1
return(linked)
}
which(linked==1)
link.check("tt0114709")
linked.check("tt0114709")
which(linked==1)
imdb.code<-"tt0114709"
linked.codes<-linked.movies(imdb.code)
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
linked.codes1<-linked.codes[which(!is.na(match(linked.codes,rownames(linked))))]
linked[linked.codes1[1:length(linked.codes1)],imdb.code]<-1
linked[imdb.code,linked.codes1[1:length(linked.codes1)]]<-1
which(linked==1)
linked<-matrix(NA, nrow=250, ncol=250)
colnames(linked)<-top250.codes
rownames(linked)<-top250.codes
linked.check<-function(imdb.code){
linked.codes<-linked.movies(imdb.code)
linked.codes1<-linked.codes[which(!is.na(match(linked.codes,rownames(linked))))]
linked[linked.codes1[1:length(linked.codes1)],imdp.code]<-1
linked[imdb.code,linked.codes1[1:length(linked.codes1)]]<-1
return(linked)
}
linked.check("tt0114709")
linked.check<-function(imdb.code){
linked.codes<-linked.movies(imdb.code)
linked.codes1<-linked.codes[which(!is.na(match(linked.codes,rownames(linked))))]
linked[linked.codes1[1:length(linked.codes1)],imdb.code]<-1
linked[imdb.code,linked.codes1[1:length(linked.codes1)]]<-1
return(linked)
}
linked.check("tt0114709")
which(linked==1)
View(data)
typeof(data$Gross)
typeof(data$Opening.Night)
sort.gross<-function(m){
title.gross<- merge(as.numeric(data$gross),as.character(data$title),by=c("row.names"))
gross.sorted<-title.gross[with(title.gross, order(-title.gross$x)),]
return (paste(Title=gross.sorted$y[1:m],":", "$",GrossAmount=gross.sorted$x[1:m]))
}
sort.gross(5)
View(title.gross)
gross.sorted<-title.gross[with(title.gross, order(-title.gross$x)),]
View(gross.sorted)
return (paste(Title=gross.sorted$y[1:m],":", "$",GrossAmount=gross.sorted$x[1:m]))
paste(Title=gross.sorted$y[1:5],":", "$",GrossAmount=gross.sorted$x[1:5])
sort.gross<-function(m){
title.gross<- merge(as.numeric(data$gross),as.character(data$title),by=c("row.names"))
gross.sorted<-title.gross[with(title.gross, order(-title.gross$x)),]
return (paste(Title=gross.sorted$y[1:m],":", "$",GrossAmount=gross.sorted$x[1:m]))
}
sort.gross(5)
